{% if item.external_theme %}

<div class='{{item.external_theme}}'>

{% endif %}

    <div class='[ wrapper ]'>   

        <div class='[ {{item.container_padding}} ] {{item.internal_theme}}'>

            <div class='[ grid {{item.grid_modifier}} ] '>

                {% for column in item.columns %}
                    
                <div class="[ col {{column_size}} ]">
                      
                    <div class='{{column.content_padding}} {{column.content_theme}}'>

                        {{ column|children }}

                    </div>

                </div>

                {% endfor %}
                
            </div>
        
        </div>

    </div>

{% if item.external_theme %}

</div>

{% endif %}